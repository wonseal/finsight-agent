# SEC EDGAR requires a descriptive User-Agent on every request.
# Format: "AppName your-contact-email"
SEC_USER_AGENT="FinSightAgent honghak0929@gmail.com"

# Optional: only needed once LLM event attribution is wired up (Day 7).
OPENAI_API_KEY=""
